import React from 'react';

const DeviceDetails = (props) => {
    return (
        <div>
            <div>
                <p>It price is : {props.price}</p>
            </div>
        </div>
    );
};

export default DeviceDetails;